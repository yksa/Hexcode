<div class="container">
<nav>
    {{#link-to 'index'}}
        Home
    {{/link-to}}

    {{#link-to 'protected'}}
        Protected
    {{/link-to}}

    {{#if session.isAuthenticated}}
        <button {{action 'logout'}}>Logout</button>
    {{else}}
        {{#link-to 'login'}}
            Login
        {{/link-to}}
    {{/if}}
</nav>

<hr/>
{{outlet}}
</div>